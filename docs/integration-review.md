# Worktree 集成检查 · 2026-09-06

## 结论

所有现有 worktree 的代码已纳入本地 main；现有未提交内容先分别提交，再保留各分支历史完成合并。四个指定任务在下述已实现范围内达到预期，已解决分支间接口和行为冲突。自动化和本机容器验证未发现剩余失败；真实设备 PET/CT 数据及 Windows 真机仍需单独验收。

| 指定任务 | 核对范围 | 结果与边界 |
| --- | --- | --- |
| `01a07444-bb05-7823-bdcb-2ffc0a2c170a` | CT 3D 去床板、自由裁剪、水模 QA、ROI 拖动、精简说明 | 原生 OpenGL 下验证状态共存、连续内外裁剪、分项重置、原始像素不变。QA 自动识别、5 ROI 拖动及切片缓存通过；按约定只显示实测指标，不判定合格。 |
| `01a06b4c-cebf-77f1-909f-21c7a61519bf` | 导航/工具 PNG、统一布局、高 DPI、安装器源码 | 保留最新两行导航、图标着色及短标签，将已实现功能从占位状态接通。窗口、侧栏、工具栏和图标测试通过。Windows 安装器尚需 Windows 真机验证；原有 DMG 属于合并前构建，本次未重新发布安装包。 |
| `01a06a0d-237a-7831-b3ec-4e96a3a5f6f5` | PET 2D、单位与上限、MPR/MIP、PET/CT 融合、手动刚性配准 | 单位事务、逐切片定量、双层 ROI、配准保存加载、真实 QML 操作通过。采用合成数据；仍保持经典单帧静态/全身 PET 的支持边界。 |
| `01a0742a-9263-7293-a13d-622e46afff3a` | PACS、数据源设置、七类显示设置、箭头标注 | Orthanc 无认证/Basic/Bearer 和 dcm4chee 实际容器验证通过，包含分页、取消、导入像素一致性、2D/MPR/Tag/原生 3D。协议范围为 DICOMweb，未新增 DIMSE。 |

此外纳入较早的序列平铺分支，以及文字箭头、单视口伪彩和视口设置分支。

## 集成时修复

- 移除平铺、融合、标注、伪彩、去床板、QA 的过期占位限制；保留尚未实现的分割、VOI 等禁用入口。
- 合并平铺与 3D/4D/Tag 的创建、切换、异步结果和关闭逻辑，保持所有视图的独立状态与资源释放。
- 统一伪彩注册表，兼容两套既有色表 ID；全局设置、单视口按钮、PET 重建与融合使用相同 LUT。PET 色表输入同步显示实际选项。
- 保留 PET 专用单位、校正信息、强度上限和双层光标，接入可配置四角信息；避免落回 CT 的 WL/WW 模板。
- 合并重复比例尺，实现全局颜色/开关与局部显示开关共同生效；恢复已打开视口的窗模板更新通知。
- 保留可编辑文字箭头和可拖动端点的普通箭头，通过标注面板切换；测量与标注分别重置。
- 去掉重复的渲染结果接收方法，统一拒绝过期结果，防止新单位/新切片被旧帧覆盖；QA/MTF 测试按真实请求 ID 交付模拟帧。
- 保留 4D 按相位缓存，同时按所选相位的实际文件和几何计算缓存指纹，避免与 PET 缓存变更相冲突。
- 补齐色条、文字标注层、伪彩面板、视口设置面板的 QRC 注册；全部 99 个资源有效，所有 QML 文件均已注册。

## 验证记录

- 完整回归：**687 passed，15 skipped**；跳过的是需显式启用的真实 PACS 测试。
- 新增跨分支回归：**3 passed**，覆盖 CT 旧帧拒绝、全局/单视口/PET 融合色表和已打开 CT 窗模板通知。
- 四个真实 PACS 入口与原生 3D：**15 passed**，补全上述跳过项。
- 最后界面调整的关联回归：**19 passed**，包括 PET 色表状态、标注与显示设置。
- `scripts/smoke_3d.py`：真实 OpenGL 的旋转、六种模板、去床板、自由裁剪、分项/全局重置、视图切换与关闭重开全部通过。
- Python 编译、QRC 编译、资源完整性、重复方法扫描、`git diff --check` 通过。

唯一现有测试警告为 VTK/NumPy 的数组 shape 弃用提示。

## 工作目录与运行环境

最终保留 `/Users/jun/Documents/git-repo/qt-dicom-viewer` 的 main 工作目录。已合并分支的提交历史保留，可通过 Git 恢复；多余 worktree 注册与对应目录在核对后删除。

PACS 的 `.env` 和合成测试数据迁至 main 的 `docker/pacs/`，继续被 Git 忽略；Bearer 网关挂载改为 main 路径，保留原有容器数据和凭据。其余服务使用 Docker 命名卷。

启动合并后的版本：

```bash
cd /Users/jun/Documents/git-repo/qt-dicom-viewer
uv run qt-dicom-viewer
# 使用本机 PACS 实验凭据：
uv run python scripts/pacs_lab.py launch
```

已经运行的旧窗口不会自动切换源码版本，需重新启动才能使用合并后的代码。
